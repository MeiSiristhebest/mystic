{
  "entities": {
    "DailyImage": {
      "title": "Daily Image",
      "description": "A shared image generated by AI for a specific prompt on a specific date.",
      "type": "object",
      "properties": {
        "imageUrl": {
          "type": "string",
          "description": "The base64 encoded image data."
        },
        "prompt": {
          "type": "string",
          "description": "The original prompt used to generate the image."
        },
        "date": {
          "type": "string",
          "format": "date",
          "description": "The date this image was generated (YYYY-MM-DD)."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the record was created."
        }
      },
      "required": ["imageUrl", "prompt", "date", "createdAt"]
    }
  },
  "firestore": {
    "/dailyImages/{imageId}": {
      "schema": "DailyImage",
      "description": "Collection of shared daily images to save Gemini API quota."
    }
  }
}
